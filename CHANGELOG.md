# Changelog

このプロジェクトの主な変更点を記録します。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [1.0.0] - 2026-08-14

最初のリリース。

### 追加

**同期エンジン**

- 各ブロックを字幕の絶対タイムコード位置に配置し、誤差が後続へ伝播する経路を排除。
  長尺でもドリフトが蓄積しない
- 尺合わせを劣化の少ない順に3段階で実施
  - 前後の無音をサンプル単位でトリム
  - 文中のポーズを優先的に圧縮（音声本体に触れないため劣化なし）
  - WSOLA による時間伸縮でピッチを保ったまま残りを吸収
- 短い音声は引き伸ばさない（枠は締切であって埋めるべき尺ではない）
- 圧縮上限を超える場合は無理に詰めず溢れさせ、次ブロックとクロスフェード

**音声整形**

- ブロック境界のフェードによるクリック音の除去
- 有声部のRMSに基づく音量の統一

**バックエンド**

- `edge` — Microsoft Edge のニューラル音声。全OS対応（既定）
- `sapi` — Windows標準のSAPI5。オフライン動作
- 同一テキストの重複合成を回避するキャッシュ

**CLI**

- `dub` — URLまたは字幕ファイルから吹き替え音声を生成
- `serve` — 生成済みプレイヤーをRange対応サーバで配信
- `voices` — 利用可能な音声を一覧
- `langs` — 動画で使える字幕言語を調査
- `verify` — 配置精度を相互相関で検証

**その他**

- YouTube URLからの字幕自動取得。人手翻訳を自動優先
- 発音辞書による読み間違いの補正（日本語辞書を同梱）
- 字幕形式は SRT / WebVTT / YouTube json3 に対応
- 同期プレイヤーの生成（シーク・倍速・一時停止に追従）
- ライブラリとしての利用に対応

### 検証済み

- 配置精度: 実動画145ブロックすべてで最大ズレ 0.000 ms
- Ubuntu / Windows / macOS × Python 3.10, 3.12, 3.13 でテスト通過

### 既知の限界

- 訳文が原語より本質的に長い区間では、同期と明瞭度がトレードオフになる
- `edge` バックエンドはネットワークが必要で、テキストがMicrosoftへ送信される
- macOS / Linux 向けのオフラインバックエンドは未実装

[1.0.0]: https://github.com/kokiaugust24th-coder/subdub/releases/tag/v1.0.0
